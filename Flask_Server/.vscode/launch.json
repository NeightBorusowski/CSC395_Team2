{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker: Flask Server",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug-flask-server",
            "python": {
                "pathMappings": [
                    {
                        "localRoot": "${workspaceFolder}/Flask_Server", 
                        "remoteRoot": "/app" 
                    }
                ],
                "projectType": "flask"
            }
        },
        {
            "name": "Docker: Ollama",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug-ollama",
            "python": {
                "pathMappings": [
                    {
                        "localRoot": "${workspaceFolder}/Flask_Server/Ollama",  
                        "remoteRoot": "/app"  
                    }
                ],
                "projectType": "flask"
            }
        }
    ]
}
